import 'package:flutter/material.dart';
import '../widget/doc_appointment_widgets.dart';

class DocAppointmentScreen extends StatelessWidget {
  const DocAppointmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const DocAppointmentScaffold();
  }
}
